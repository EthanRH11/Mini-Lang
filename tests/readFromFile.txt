needs: {
    source: "readFromFile.hmlng"
}

proc myFoo (int x) => {
    x = x * 2;
    result => {x};
}

begin:
    int myInt = 12;
    out_to_console(myFoo(myInt));

    out_to_console(myFoo(myInt));
    
end