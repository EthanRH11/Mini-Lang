@once

object => ball() {
available:
    default::ball();
    factory::ball() => {ball};
    method::calcArea(int x, int y) => {
        result => {x * y};
    }
secure:
    takes int x;
    takes double y = 0;
}

@last