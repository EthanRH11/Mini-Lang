// This is a sample program for testing the lexer
>>$ This is a single line comment

<<$ This is a 
multi-line comment that spans
multiple lines $>>

// Testing keywords and operators
if (x == 5) {
    print('\n');
    x += 10;
}
else {
    y++;
}

// Testing array declarations
array<int> numbers;
array<double> values;

// Testing input types
<int> age;
<str> name;

// Testing operators
a + b * c / d;
x = (y >= 10) ? y : z;
result = (a =/= b);

// Testing a more complex expression
while (count-- > 0) {
    if (value => threshold) {
        print("Above threshold");
    }
}

// Testing special newline symbols
print('\n');
print("Line one...");
print("Line two...");